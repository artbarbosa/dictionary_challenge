name: dictionary
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  design_system:
    path: ./design_system

  collection: ^1.17.2
  cupertino_icons: ^1.0.2
  equatable: ^2.0.5
  flutter_dotenv: ^5.1.0
  flutter_tts: ^3.8.3
  get_it: ^7.6.4
  google_fonts: ^6.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.1
  mocktail: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/.env
    - assets/english_frequency_10k.csv
    - test/mock/mock_list_word.csv